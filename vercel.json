{
  "buildCommand": "cd \"./company level \" && npm install && npm run build",
  "outputDirectory": "company level /dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
